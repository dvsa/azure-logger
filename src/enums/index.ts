export enum LOG_LEVELS {
  CRITICAL = 'critical',
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info',
  DEBUG = 'debug',
  SECURITY = 'security',
  AUDIT = 'audit',
  EVENT = 'event',
  REQUEST = 'request',
  DEPENDENCY = 'dependency',
}

export enum APP_INSIGHTS_LOG_LEVELS {
  TRACE = 'trace',
  EXCEPTION = 'exception',
  EVENT = 'event',
  REQUEST = 'request',
  DEPENDENCY = 'dependency',
}
